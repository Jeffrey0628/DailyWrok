# SQL
# 6 过滤数据
## 使用WHERE子句
1. SELECT xx FROM xx WHERE x = y;
## WHERE子句操作符
| 操作符     | 说明     |
| :--- | :-------- |
| =  | 等于 |
| <> |不等于|
| != |不等于|
| <  |小于|
| >  |大于|
| <= |小于等于|
| >= |大于等于|
|BETWEEN|在指定范围之间|

判断空值：SELECT xx FROM xx WHERE xx IS NULL;

# 7 数据过滤
## 组合WHERE子句
1. AND
2. OR
3. IN： WHERE x IN （y, z）
4. NOT

# 8 通配符进行过滤
## LIKE操作符
1. 百分号%，匹配多个字符多次，区分大小写：WHERE xx LIKE 'jet%'
2. 下划线通配符_，匹配一个字符多次

# 9 用正则表达式进行搜索
## REGEXP关键字
1. MySQL实现了一个很小的正则

# 10 创建计算字段
## 拼接字段
1. MySQL使用Concat()函数来拼接字段
2. RTrim()函数，去掉值右边的所有空格；LTrim(),Trim()
## 使用别名
1. AS
## 数学计算
1. 直接SELECT接表达式即可

# 11 使用数据处理函数
## 处理函数
1. Upper(), Left(), Length(), Locate(), Lower(), Soundex(), Substring()
2. 日期：Date(), SELECT xx FROM xx WHERE Date(x) BETWEEN 'y' AND 'z'
3. 数值处理函数，Abs(), Cos(), Exp(), Mod(), Pi(), Rand(), Sin(), Sqrt(), Tan()

# 12 汇总数据
## 聚集函数
1. AVG(), 只针对单行，忽略NULL
2. COUNT(), 统计非NULL数量
3. MAX(), MIN(), SUM(), 
4. DISTINCT关键字，使用AVG

# 13 分组数据
1. GROUP BY
2. GROUP BY + HAVING子句
3. GROUP BY + ORDER BY
4. WHERE在分组前计算，HAVING在分组后计算
5. 

# 14 使用子查询
1. 利用子查询进行过滤，也就是子查询嵌套
2. 作为计算字段使用子查询

# 15 联结
1. 等值联结：WHERE子句，用全名；或者多个表里取
2. 也成为内部联结INNER JOIN
3. **SELECT xx FROM x INNER JOIN y ON z1.z = z2.z** 

# 16 高级联结
1. 自联结：先取别名，再用别名当俩表就行了；自联结处理速度一般比子查询要快。
2. 自然联结：相同的列只出现一次
3. 外联结：**FROM x LEFT/RIGHT OUTER JION y** ON ....

# 17 组合查询
1. UNION规则：a.由两条或两条以上的SELECT语句组成，语句之间用关键字UNION分隔；UNION中的每个查询必须包含相同的列、表达式或聚集函数；列数据类型必须兼容
2. UNION会去重，UNION ALL不会
3. UNION中的ORDER BY，会排序所有的SELECT

# 18 全文本搜索
1. 创建的时候，通过FULLTEXT子句建立索引
2. SELECT x FROM x WHERE Match(y) Against ('z')
3. 查询扩展 QUERY EXPANSION

# 19 插入数据
1. INSERT INTO Customers VALUES(x, y, z...)
2. 插入多条数据，用分号;分隔
3. INSERT INTO customers(x, y, z....) VALUES(x, y, z...) 一一对应
4. INSERT **LOW_PRIORITY** INTO

# 20 更新和删除数据
1. UPDATE x SET y1=y2 WHERE z1=z2
2. DELETE x WHERE y1=y2
3. 更快的删除，使用**TRUNCATE TABLE**, 删除原来的表并重新创建一个表，而不是逐行删除表中的数据

# 21 创建和操作表
1. CREATE TABLE customers(cust_id int NOT NULL AUTO_INCREMENT, xx)
2. 使用NULL值
3. PRIMARY
4. AUTO_INCREMENT
5. 指定默认值，默认值不能为函数，尽量使用默认值而不是NULl
6. ALTER TABLE vendors ADD vend_phone CHAR(20);
7. ALTER TABLE vendors DROP vend_phone;
8. ALTER TABLE orderitems ADD CONSTRAINT fk_xx FOREIGH KEY (y) REFERENCES orders (z)
9. 删除表DROP TABLE customers2
10. 重名名表RENAME TABLE customers2 TO customers;

# 22 使用视图
# 23 使用存储过程
1. 